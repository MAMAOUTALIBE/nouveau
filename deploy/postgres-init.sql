-- Exécuté automatiquement par l'image PostgreSQL à la première initialisation.
-- Le schéma applicatif `hr` doit exister avant que les migrations Alembic
-- ne s'appliquent au démarrage du backend.
CREATE SCHEMA IF NOT EXISTS hr;
